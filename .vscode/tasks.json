{
	"version": "2.0.0",
	"tasks": [
		{
			"type": "cppbuild",
			"label": "ft_printf test",
			"command": "/usr/bin/gcc",
			"args": [
				"-fdiagnostics-color=always",
				"-g",
				"${file}",
				"-o",
				"${fileDirname}/${fileBasenameNoExtension}.out",
				"-lftprintf",
				"-L${workspaceFolder}/",
				"-Wall",
				"-Wextra",
				"-Werror"
			],
			"options": {
				"cwd": "${fileDirname}"
			},
			"problemMatcher": [
				"$gcc"
			],
			"group": {
				"kind": "build",
				"isDefault": true
			},
			"detail": "compilador: /usr/bin/gcc"
		}
	]
}
